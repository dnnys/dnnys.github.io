name: Build and deploy Jekyll site

on:
  push:
    branches: [ dev ]

jobs:
  github-pages:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: helaili/jekyll-action@v2
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          # Optional if your Jekyll site needs libvips
          pre_build_commands: sudo apt-get update && sudo apt-get install -y libvips libvips-dev
